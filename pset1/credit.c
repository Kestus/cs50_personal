#include <cs50.h>
#include <stdio.h>
#include <string.h> //strlen(s)


int main(void)
{
    long card_number;
    bool valid;
    string card_type;
    int card_length;

    //get card number
    do
    {
    card_number = get_long("Number: ");
    } while (card_number > 0);
    //convert long to string/array (???)
    (string)card_number;



    //check valid
    //for (card_length = 0; card_number[card_length] != '\0'; card_length++){}
    for()


    printf("%i\n", card_length);

}


//get number

//check valid

    //check length

    //build % remainder

//check type

//print result card type